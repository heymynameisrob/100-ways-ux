<!DOCTYPE html>
<html lang="en" class="font-sans text-gray-700 tracking-tight antialiased">
  <head>
   {% include "head.njk" %}
  </head>
  <body>    
    {% include "nav.njk" %}
    <header role="banner" class="relative pt-16 px-4 text-left bg-black bg-cover" style="background-image:url({{featuredImg}});">
      <div class="absolute top-0 left-0 w-full h-full bg-black bg-opacity-25"></div>
      <div class="relative z-10 container bg-white rounded-t-2xl pt-24">
        <div class="flex items-center flex-col space-y-4 md:space-y-6">
          <small class="py-1 px-3 uppercase font-bold text-sm bg-red-100 text-red-900 rounded-full md:text-base">{{type}}</small>
          <h1 class="text-3xl md:text-4xl lg:text-5xl font-bold text-gray-900 lg:leading-tight tracking-tighter mb-4 md:mb-6 lg:mb-8">{{ title }}</h1>
          <p class="text-xl md:text-2xl md:leading-normal lg:leading-normal text-gray-700 mb-4 md:mb-6 lg:mb-8">By Rob Hough</p>
        </div>
      </div>
    </header>
    <section class="blog-content container">      
      {% include "sections/inline-offer.njk" %}
      <div class="max-w-2xl mx-auto px-4 prose lg:prose-lg xl:prose-xl">
        {% block content %}
          {{ content | safe }}
        {% endblock %}
      </div>
    </section>
    {% include "footer.njk" %}
  </body>
</html>